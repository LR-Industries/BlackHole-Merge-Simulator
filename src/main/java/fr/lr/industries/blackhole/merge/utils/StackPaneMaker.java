package fr.lr.industries.blackhole.merge.utils;

// Import the required classes and packages
import javafx.scene.layout.StackPane;

// The StackPaneMaker interface is used to create a stack pane
public interface StackPaneMaker {
    // Create a method to get the StackPane
    StackPane getStackPane();
}
